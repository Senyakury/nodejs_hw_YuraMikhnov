doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    meta(name="color-scheme", content="light dark")
    style.
      body {
        min-height: 100vh;
        margin: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction:column;
        background-color:#66B8D9;
        color: black;
        overflow: auto; 
      }
      .container {
        height: 350px;
        width: 300px;
        background:#D0D7D9;
        padding: 30px;
        box-shadow: 0px 0px 20px black inset;
        border-radius: 10px;
        display:flex;
        flex-wrap:wrap;
        align-items:center;
      }
      form {
        margin: auto;
        display: flex;
        flex-wrap: wrap;
        gap: 25px;
      }
      button {
        width: 170px;
        margin: auto;
        height: 40px;
        background-color:#66B8D9;
        color:black;
        border: none;
        border-radius: 5px;
        transition: 0.4s;
        cursor:pointer;
      }
      button:hover{
        box-shadow: 0px 0px 7px black inset;
        box-shadow: 0px 0px 7px black;
      }
     header {
        height:70px;
        width:100%;
        position:absolute;
        bottom:89.5%;
        background-color:rgb(49, 18, 95);
        display:flex;
        justify-content:flex-end;
        align-items:center;
        column-gap:20px;
        color:#d796e0;
      }
      header span{
        transition:0.4s;
      }
      header a{
        background-color:#66B8D9;
        width:140px;
        height:50px;
        margin-right:20px;
        display:flex;
        justify-content:center;
        align-items:center;
        transition:0.4s;
      }
      header span:hover{
        font-size:18px;
      }
      header a:hover{
        box-shadow:0px 0px 10px black;
        font-size:18px;
      }
      main>span{
        font-size:34px;
        margin:auto;
        text-align:center;
      }
  body(style="min-height: 100vh;")
    header
      span About Us
      a(href="/main") Back to main
    main.container
      span Are you sure you want to delete this User?
      form(action=`/main/profile/deleteuser/${id}`, method="POST") 
        button(type="submit")  Delete User